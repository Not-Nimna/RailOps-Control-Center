# React + TypeScript + Vite

## Install Dependencies

From the frontend project folder, run:

```bash
npm install leaflet react-leaflet recharts lucide-react
npm install -D @types/leaflet
```

to run the dev server

```bash
cd frontend
npm run dev
```
